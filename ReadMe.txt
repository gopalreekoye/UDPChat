To compile app, run type make into the terminal in the root directory of the project:
    ~/ make

To run the server:
    ~/ make start-server

To run the client:
    ~/ make start-client

To run multiple clients you need to use multiple terminals and run the start client command on the.

**Always start the server first before the client

To clean old class files:
    ~/make clean